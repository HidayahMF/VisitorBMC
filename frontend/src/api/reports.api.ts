import { apiClient } from './client';
export interface VisitReportRow { VisitCode:string; VisitDate:string; VisitorName:string; CompanyName:string; HostName:string; CheckInTime:string|null; CheckOutTime:string|null; Status:string; }
export async function getVisitReport(params: Record<string,string|number|undefined>) { const q=new URLSearchParams(); Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!=='')q.set(k,String(v));}); return apiClient<{data:VisitReportRow[];pagination:{total:number;page:number;totalPages:number}}>(`/reports/visits?${q}`); }
export async function getVisitorReport(params: Record<string,string|number|undefined>) { const q=new URLSearchParams(); Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!=='')q.set(k,String(v));}); return apiClient<{data:Record<string,unknown>[];pagination:{total:number;page:number;totalPages:number}}>(`/reports/visitors?${q}`); }
export async function getInductionReport(params: Record<string,string|number|undefined>) { const q=new URLSearchParams(); Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!=='')q.set(k,String(v));}); return apiClient<{data:Record<string,unknown>[];pagination:{total:number;page:number;totalPages:number}}>(`/reports/inductions?${q}`); }
export function toCsv(rows: Record<string, unknown>[]) { if (!rows.length) return ''; const keys=Object.keys(rows[0]); return [keys.join(','),...rows.map(row=>keys.map(key=>`"${String(row[key]??'').replaceAll('"','""')}"`).join(','))].join('\n'); }
export function downloadCsv(filename: string, rows: Record<string, unknown>[]) { if (!rows.length) return; const link=document.createElement('a'); link.href=URL.createObjectURL(new Blob([toCsv(rows)],{type:'text/csv;charset=utf-8'})); link.download=filename; link.click(); URL.revokeObjectURL(link.href); }
